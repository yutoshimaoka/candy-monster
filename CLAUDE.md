> このファイルは**原則の宣言**です。HTML / CSS / JS / 画像の具体的なルールは `.claude/rules/` 配下に書き、ここでは繰り返しません（二重管理を避けるため）。

## 守るべき原則

- デザイン値（カラー・余白・サイズ）は直値を書かず、`DESIGN.md` の `:root` 変数を使う
- class 名は BEM で統一する（粒度の目安は `.claude/rules/css.md` を参照）
- セマンティックなマークアップとアクセシビリティを常に優先する
- バリアブル未設定のデザイン値は、`DESIGN.md`の一覧に無いものを使わない。新しい値が必要な場合は先に一覧へ追加してから使う
- 1セクション実装したら、スクリーンショットを撮ってFigmaと比較する
- Lint（stylelint / eslint / htmlhint）のエラーをゼロにしてからコミットする
- push対象の参照はブランチ名ではなくコミットハッシュで固定する

## Claude の作業スタイル（Opus 5 向け）

> 出典：Anthropic「Prompting Claude Opus 5」。Opus 5 は自律的に完遂・検証・委譲するため、**足りない指示を足すより、過剰さを抑える**のが要点。ただし下記「守るべき原則」の品質ゲートは維持する。

- **応答は簡潔に**：結論から書き、前置き・但し書きは短く。説明を求められたら、明示がない限りまず要点を示す
- **進捗ナレーションは絞る**：着手前に何をするか1文 → 作業中は重要な発見・方針転換のときだけ更新 → 最後は「何をしたか／何が分かったか」を先頭に述べる
- **スコープを厳守**：頼まれた範囲・粒度で仕上げる。解釈が分かれて成果物が大きく変わる時だけ確認する。勝手に狭めず・広げず・別物にしない。より良い案があれば1文添えて、頼まれた通りに進める
- **冗長な自己検証・再確認を足さない**：Opus 5 は自分で検証・自己修正する。「最終検証ステップを入れよ」「再確認せよ」等の指示は不要。ただし**このプロジェクトの品質ゲート（Lint ゼロ／セクションのスクショ確認／Lighthouse／コントラスト実測）は必須なので維持する**
- **書き出す文書は必要な長さに**：水増し・重複要約・定型文で膨らませない
- **自己訂正の言及は最小限**：ユーザーのコード・結論・判断が変わる誤りだけ簡潔に訂正。影響のない修正は黙って直して進む
- **サブエージェントは乱発しない**：本当に独立した大きなタスク（広範な多ファイル調査など）だけ委譲。数手で終わる作業や自己検証には使わない。ユーザーが明示的に指示したときのみ使う
- **effort は低め主体**：品質が保てる範囲で `low`／`medium` を主に使い、要求の高いコーディング・エージェント作業でのみ上げる

## トラブルシュートの自動記録

- エラー・警告・想定外の挙動でつまずき、それを解決した場合は、**その都度 `TROUBLESHOOTING.md` に追記する**（ユーザーが指示しなくても自動で行う）
- 記録は「症状／原因／対処法」の3項目で書く。原因が不明な場合は「原因：不明」と残す
- STEP1（環境構築・MCP接続）の段階から記録を始める
- 同じ症状が既に記載されている場合は重複追記せず、追加情報があれば補足する

## 参照先

- デザイン値（カラー・タイポ・スペーシング）は `DESIGN.md` を参照
- HTML / CSS / JS / 画像のルール詳細は `.claude/rules/` 配下を参照
- セクション完了条件は `.claude/skills/lp-implementation/SKILL.md` の Definition of Done を参照
