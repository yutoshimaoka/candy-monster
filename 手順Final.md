# LP 制作フロー & 実装ガイドライン（Final・自動進行版）

Figma データから**迷いなく・プロの実装レベル**で LP を完成させるための手順書です。
準備 → 実装 → 品質チェックの流れに沿って進めます。
この手順書は、`lp-start` による自動進行を前提とした**簡易版（Final）**です。各STEPで送る個別メッセージは記載していません（Claude Code が各スキルを自動で参照します）。手動で送るメッセージ例が必要な場合は `新手順.md` を参照してください。

---

# 🗺️ 全体の流れ

```
STEP 0  Figmaを整理する
STEP 1  プロジェクトの初期セットアップ（テンプレートをクローン → Vite / Lint 導入）
STEP 2  Figma MCPを接続する
STEP 3  セクションごとに読み込ませる → 要約させて人間が承認
STEP 4  バリアブルを読み込ませる（無ければ値を一覧化して登録）
STEP 5  README.md / CLAUDE.md / DESIGN.md を更新する
STEP 6  セクションごとに実装 → スクショ比較 → 完了条件確認 → コミット
STEP 7  Lighthouse計測・アクセシビリティチェック
STEP 8  GitHubリポジトリ作成・作業ブランチへアップロード
STEP 9  PR作成 → レビュー依頼 → 対応 → マージ → 共有URL発行
```

> **原則**：STEP 0〜5 は「準備・読み込み」のみ。Claude Code にはまだLPのコードを書かせません（STEP1の環境構築用コマンドは除く）。

---

## STEP 0：Figma を整理する

- データは **Auto Layout** で組む
- レイヤー名を以下のルールで付け直す

| 種類 | 接頭辞 | 例 |
|------|--------|-----|
| 画像 | `Image /` | `Image / hero-bg` |
| ロゴ | `Logo /` | `Logo / company` |
| アイコン | `Icon /` | `Icon / arrow` |

**追加チェック（品質のばらつき防止）**

- [ ] 繰り返し要素（カード、ボタン等）が **Component / Variants** 化されている
- [ ] テキストにダミー文言（Lorem ipsum 等）が残っていない（本文コピー未反映を防ぐ）
- [ ] PC / SP のフレーム名にサフィックスを統一している（例：`Hero – PC` / `Hero – SP`）

---

## ▶️ はじめに：Claude Code で「lp-start」と言う

STEP0（Figma整理）まで済んだら、テンプレートをクローンした状態で Claude Code を開き、まず次の起動ワードを送ります。

```
lp-start
```

これは進行管理スキル（`start-orchestrator`）を呼び出す合図です。全STEPの一覧と「いま自分がどこにいるか」がスタンプラリー形式で表示され、**開始前の確認は挟まず、次にやるべきSTEPをSTEP順にそのまま実行します**（順番が前後してSTEP3以降の依存が崩れるのを防ぐため）。各STEPが終わると進捗（`PROGRESS.md`）が自動更新され、**「次のSTEPに進んでよろしいですか？（YES / NO）」と確認**したうえで次へ進みます。

## STEP 1：プロジェクトの初期セットアップ

Figmaを読み込む前に、実装の土台を作ります。ここで構成を固めておくと、後から「Viteどうする？」「Lintいつ入れる？」と迷わずに済みます。

### 1. テンプレートをクローンする

実装ガイドライン（`CLAUDE.md` / `DESIGN.md` / `.claude/` 配下）が揃ったテンプレートから始めます。

```bash
# 「Use this template」で新規リポジトリを作るか、クローンして履歴を切り離す
git clone <テンプレートのURL> my-lp-project
cd my-lp-project
rm -rf .git && git init
```

### 2. プロジェクト構成を整える

以下をこのタイミングで導入します。実装を始めてから追加すると設定がばらつくため、**必ず実装前に**済ませます。

- [ ] **Vite を導入する**（バンドラを最初に固定し、ビルド設定を統一する）
- [ ] **`.gitignore` を追加する**（`node_modules` / `.env` をリポジトリに含めない）
- [ ] **リセット CSS または `box-sizing: border-box` を適用する**
- [ ] **Lint を導入する**：`stylelint`（BEM崩れ・直値検知）/ `eslint`（JS）/ `htmlhint`（セマンティック・alt漏れ）
- [ ] **`pre-commit` フックを設定する**（コミット前に Lint が自動で走るようにする）

## STEP 2：Figma MCP を接続する

### まず接続状態を確認する（済んでいればスキップ）

このSTEPは、**すでに接続済みなら何もしなくてOK**です。`figma-mcp-connect` スキルが最初に接続状態を確認し、次のように分岐します。

- **接続済み** → このSTEPをスキップし、`PROGRESS.md` の STEP2 を `[x]` にして次へ進む
- **未接続** → 下記の手順（①または②）で接続する

### ① リモート版MCPサーバー（プラグイン経由・推奨）

Figma公式が「機能が最も幅広い」として推奨している方法です。

```bash
claude plugin install figma@claude-plugins-official
```

**接続手順**

1. Claude Code を再起動する
2. `/plugin` を実行 → **Installed** タブへ移動
3. `figma` を選択して Enter → 認証ページが開く
4. ブラウザで **Allow access** をクリック
5. ターミナルに戻り、再度 `/plugin` を実行して `connected` 表示を確認

> ⚠️ **ハマりやすいポイント①**：MCP接続は起動時に初期化される。接続後にリストへ出てこない場合は Claude Code を完全に再起動する。

> ⚠️ **ハマりやすいポイント②（フォールバック）**：環境によっては `claude-plugins-official` マーケットプレイスが自動登録されておらず、`Plugin "figma" not found in marketplace` というエラーで失敗することがある。その場合は以下を先に実行してからインストールし直す。
>
> ```bash
> /plugin marketplace add anthropics/claude-plugins-official
> ```

### ② デスクトップ版MCPサーバー（代替手段）

Figmaデスクトップアプリを使っている場合、Dev Modeパネルから直接ローカルMCPサーバーを有効化する方法もあります。

1. Figmaデスクトップアプリでファイルを開き、Dev Modeに切り替える
2. 右サイドバーでMCPサーバーを有効化し、表示されたURL（例：`http://127.0.0.1:3845/mcp`）をコピーする
3. ターミナルで以下を実行する

```bash
claude mcp add --transport http figma-desktop http://127.0.0.1:3845/mcp
```

4. Claude Code を再起動し、`/mcp` で接続を確認する

---

## STEP 3：Figma URL をセクションごとに読み込ませる

セクション単位（Hero、Features…）で PC・SP 両方のリンクを渡します。

**フレームリンクの取得方法**

1. 対象のフレームを選択する
2. 右クリック → **コピー/貼り付けオプション** → **選択範囲へのリンクをコピー**（`⌘L`）
3. セクションごとに PC・SP 2つ分のリンクを取得する

## STEP 4：バリアブルを JSON で読み込ませる（無ければ値を一覧化して登録）

**バリアブルがある場合**

1. バリアブルパネルを開く
2. 対象のコレクション（Color、Size など）を右クリック → **エクスポートモード**
3. 書き出した JSON を読み込ませる。**この段階ではまだ実装させない**

## STEP 5：README.md / CLAUDE.md / DESIGN.md を更新する

**ドキュメント更新**

テンプレートをクローンしているため、以下のファイルは既に配置済みです。中身をこのプロジェクト用に更新します。

| ファイル | 役割 |
|---------|------|
| `README.md` | プロジェクト概要、Figma URL、セットアップ・起動手順 |
| `CLAUDE.md` | Claude への行動指示のみ |
| `DESIGN.md` | デザイントークン、カラー、タイポグラフィ、レイアウト規則 |

## STEP 6：セクションごとに実装する（スクショ比較 + 完了条件付き）

以下をワンセットとして、セクション単位（Hero → Features → …）で繰り返します。

```
① 実装ガイドラインに沿って実装する
② 実装画面のスクリーンショットを撮る
③ Figmaのフレームと並べて比較する（ズレ・余白・フォントサイズ）
④ 差分があれば修正する
⑤ 下記「完了条件」を満たしているか確認する
⑥ セクション単位でコミットする
⑦ 次のセクションへ進む
```

### セクション完了条件（Definition of Done）

- [ ] Figmaのスクリーンショットと実装画面を比較し、ズレがない
- [ ] PC / SP 両方のブレークポイントで崩れがない
- [ ] 実装ガイドラインのチェックリスト（該当項目）を満たしている
- [ ] Lint / 自動チェックがエラーなしで通っている
- [ ] コンソールエラーが出ていない
- [ ] セクション単位でコミット済みである

## STEP 7：Lighthouse 計測・アクセシビリティチェック

全セクションの実装が完了したら、公開前に品質を数値で確認します。

- [ ] Lighthouse でパフォーマンス・アクセシビリティ・SEOスコアを計測する
- [ ] 目標スコアを下回った項目があれば対応する（目安：パフォーマンス90以上／アクセシビリティ95以上）
- [ ] 主要なアクセシビリティ違反（コントラスト比、aria属性漏れ等）がないか確認する

## STEP 8：GitHub リポジトリ作成と作業ブランチへのアップロード

### 8-1. gh（GitHub CLI）を確認する（無ければ導入）

- [ ] `gh --version` でインストール済みか確認する（入っていればスキップ）
- [ ] 未インストールなら導入する

```bash
brew install gh      # Mac (Homebrew)
```

- [ ] `gh auth status` で認証済みか確認する。未認証なら `gh auth login`（ブラウザ承認）

> `gh` を使わず、リポジトリ作成・PR作成をブラウザで行う場合はこのステップは不要です。ただし Claude Code に GitHub 操作を任せるなら `gh` があるとコマンドで完結できます。

### 8-2. GitHub リポジトリを作成する（未作成の場合）

- [ ] **リポジトリ作成の前に、プロジェクト名を英語で決める**（Claude Code に任せる場合は「プロジェクト名を英語で入力してください」と聞かれるので、英語名で答える）
  - 使える文字：半角英数字・ハイフン `-`・アンダースコア `_`（スペースや日本語は不可）
  - 慣習的に**小文字＋ハイフン区切り（kebab-case）**が読みやすい。例：`project-a-lp`
  - スペース・日本語を入れた場合、Claude Code がハイフン区切りの英語への修正案を提示するので、それで確定するか別名を入力する
- [ ] **同じ名前のリポジトリが既に存在しないか確認する**（`gh repo view <Organization名>/<リポジトリ名>` で確認）
  - この確認は挙動が直感に反する点に注意：**既に存在すると情報が返り、存在しないと 404 エラーが返る（＝エラーが出る方が「作成OK」）**
  - 既に存在する場合：「同じ名前のリポジトリが存在します。別の名称にしてください。」→ 別名を決め直す（重複がなくなるまで繰り返す）
  - 存在しない場合：その名前をそのままリポジトリ名にする
- [ ] GitHub で新規リポジトリを作成する（`gh` CLI で作成）
- [ ] **リポジトリは初期状態（README等を追加しない空の状態）で作る**。すでにローカルに実装（コミット履歴）があるため、GitHub 側にファイルを足すと履歴が食い違い、後の push で衝突（rejected）する。空で作ればローカル履歴をそのまま push できる

```bash
# 作成前に同名チェック（存在すれば情報が返り、無ければエラー＝作成OK）
gh repo view <Organization名>/<リポジトリ名>

# GitHub CLI で作る場合（Internal・現在のフォルダを紐付け）
# <リポジトリ名> は決めたプロジェクト名（英語）を使う
gh repo create <リポジトリ名> --visibility internal --source=. --remote=origin

# 手動で作った場合は、リモートを追加
git remote add origin <リポジトリのURL>
```

### 8-3. アップロードしてよいか確認する

- [ ] リポジトリ作成が完了したら、アップロードしてよいか確認する（Claude Code に任せる場合は「アップロードしますか？（YES / NO）」と聞かれる）
  - YES → push に進む／NO → 止まって指示を待つ

### 8-4. 作業ブランチで push する（main には直接入れない）

- [ ] 変更をコミットする（セクション単位のコミットのまま）
- [ ] **初回から `main` に直接 push せず、作業ブランチを切って push する**（レビューを通すため）

```bash
git switch -c feature/lp-initial   # 初回。2回目以降は feature/hero-fix など変更内容の名前
git push -u origin feature/lp-initial
```

> このあと STEP9 で PR を作成し、レビューを経て `main` にマージします。共有用URL（GitHub / Pages）は、初回 PR がマージされてから STEP9 で発行します。

> ⚠️ push 前に秘密情報が含まれていないか確認する。`.env`・APIキー・トークンが `.gitignore` で除外済みか `git status` で確認する。
> ⚠️ Figma の個人アクセストークン（`figd_` で始まる文字列）などをコミットに含めない。

## STEP 9：変更を PR で出し、レビューを経てマージする

初回アップロード（STEP8）は `main` に直接入れました。**それ以降の変更は、`main` に直接 push せず、作業ブランチ → PR → レビュー → マージ**の流れで行います。変更のたびにこのSTEPを繰り返します（1変更＝1PR が基本）。

> 💡 9-1〜9-5（PR作成・レビュー依頼・レビュー対応・マージ・共有URL発行）は、すべて Claude Code が `gh` で実行できます。人間が行うのは「レビュワーとしてコメントを書くこと」と「マージ前の最終承認（YES / NO）」です。
> 作業ブランチの作成・push は STEP8（8-4）で完了済み。このSTEPは PR 作成から始めます。

### 9-1. Pull Request を作成する

- [ ] `main`（デフォルトブランチ）向けに PR を作成する
- [ ] PR タイトルは変更内容がわかるように書く（例：「LP初版：Hero〜Footer」「Hero：見出し余白を修正」）
- [ ] PR 説明欄に以下を記載する
  - 変更したセクション・内容の概要
  - 実装ガイドラインのチェックリスト（該当項目）
  - Figma の参照 URL
  - レビューで特に見てほしい点（レビュー観点）

```bash
gh pr create --base main --title "<変更内容>" --body "<説明>"
```

### 9-2. レビューを依頼する

- [ ] レビュワーを指定し、PRのURLを連絡する（PRを作っただけではレビュワーは気づかない）
- [ ] **レビュワーのコメントは、この PR 上に書いてもらう**（表示確認用のURLではなく PR に集約する）

```bash
gh pr edit <PR番号> --add-reviewer <レビュワー名>
gh pr view <PR番号> --json url --jq .url        # 連絡用のPR URLを取得
```

### 9-3. レビュワーのコメントURLを Claude Code に渡して対応する

- [ ] レビュワーが付けたコメント（またはPR）のURLを Claude Code に渡す
- [ ] 指摘を仕分け（すぐ直す / 相談 / 別PR）→ 指摘ごとにコミットを分けて修正・push → 各スレッドに1件ずつ返信
- [ ] 対応後、**まとめて1回**再レビューを依頼し、Slack共有サマリ（PR URL＋対応概要 各1〜2行）を出力する

```bash
gh pr view <PR番号またはURL> --comments        # 指摘を全件洗い出す
gh pr edit <PR番号> --add-reviewer <レビュワー名>   # 再レビュー依頼
```

> 詳しいレビュー対応（仕分け・返信・Slack共有サマリ）は `review-response` スキル、または「レビュー対応の実務ガイド.md」を参照。

### 9-4. マージする

- [ ] マージ前に確認する（Claude Code に任せる場合は「この PR をマージしますか？（YES / NO）」と聞かれる）
- [ ] YES の場合、次を満たしてからマージする
  - 必要な Approve が揃っている
  - レビュー指摘がすべて解消（または相談済み）で、会話が resolve されている
  - CI（`gh pr checks`）が通っている

```bash
gh pr checks                            # CI/Lint の結果を確認
gh pr merge --squash --delete-branch    # squashマージし、マージ済みブランチを削除
```

> マージ方式はリポジトリの運用に合わせる（`--squash` / `--merge` / `--rebase`）。指定が無ければ `--squash` を既定にする。

### 9-5. チームへの共有URLを発行する（初回マージ後に一度／GitHub URL / Pages URL）

- [ ] 初回の PR が `main` にマージされたら、GitHub Pages を有効化して共有URLを発行する（2回目以降は既に有効なので不要）
- [ ] GitHub URL と Pages URL の2つをチームに共有する

```bash
# main ブランチのルートを公開ソースにして Pages を有効化
gh api -X POST repos/<Organization名>/<リポジトリ名>/pages \
  -f "source[branch]=main" -f "source[path]=/"

# 公開URL / リポジトリURL を取得
gh api repos/<Organization名>/<リポジトリ名>/pages --jq .html_url
gh repo view <Organization名>/<リポジトリ名> --json url --jq .url
```

- チームに共有する2つのURL：
  - GitHub URL（リポジトリ）：`https://github.com/<Organization名>/<リポジトリ名>`（コード確認用）
  - Pages URL（公開ページ）：`https://<Organization名>.github.io/<リポジトリ名>/`（表示物の確認用）

> 反映まで数十秒〜数分かかることがあります。Internal リポジトリの Pages は公開範囲が Organization 内に限定されます。

### 9-6. Claude Code に一括で任せる場合

9-1〜9-5（PR作成 → レビュー依頼 → レビュー対応 → マージ → 共有URL発行）を通しで任せられます。送るメッセージ例：

```
この変更を、PR作成からマージ・共有URL発行まで進めてください。
- main 向けにPRを作成し、説明欄に「変更概要・チェックリスト・Figma URL・レビュー観点」を記載する
- レビュワーを指定し、連絡用のPR URLを出す
- 私がレビュワーのコメントURLを送るので、1件ずつ対応し、修正はコミットして再レビューを依頼する（迷うものは相談）。Slack共有サマリも出す
- レビュー完了後、「マージしますか？（YES/NO）」と確認し、YESなら Approve・resolve・CI通過を確認して gh pr merge する
- 初回マージ後は GitHub Pages を有効化し、GitHub URL と Pages URL を返す
```

> 💡 レビュー指摘のうち、コーディング規約に関するもの（直値・BEM・セマンティックタグ等）は、そもそも `.claude/rules/` と Lint で防げるものが多い。繰り返し同じ指摘が出る場合は、ルール側に反映して再発を防ぐ。

### 完了条件（Definition of Done）

- [ ] PR が作成され、説明欄に必要情報（変更概要・チェックリスト・Figma URL・レビュー観点）が記載されている
- [ ] レビュワーを指定し、PR URL で依頼した
- [ ] レビュー指摘にすべて対応（または相談）済みで、会話が resolve されている
- [ ] 再レビューをまとめて1回依頼し、Slack共有サマリ（PR URL＋対応概要）を出力した
- [ ] 必要な Approve と CI 通過を確認した
- [ ] ユーザーの YES を得てからマージした
- [ ] （初回のみ）マージ後に GitHub Pages を有効化し、共有URL（GitHub / Pages）を発行した

## 📋 よくあるトラブルと対処法

| 症状 | 原因 | 対処法 |
|------|------|--------|
| MCPが接続したのに`/plugin`のリストに出てこない | 起動時にしか初期化されない | Claude Codeを完全に再起動する |
| `claude plugin install figma@claude-plugins-official`が「マーケットプレイスが見つからない」で失敗する | `claude-plugins-official`が環境によって自動登録されていない | `/plugin marketplace add anthropics/claude-plugins-official`を先に実行してから再インストール |
| Figmaと全く違う見た目で実装される | 権限エラーで取得できておらず推測実装された | STEP3の要約確認を徹底し、閲覧権限を確認して再実行 |
| デザイン変更のたびに複数箇所を修正している | 直値（マジックナンバー）で書かれている | DESIGN.mdの`:root`変数を必ず使うルールを徹底する |

---

# 🧩 実装ガイドライン

## 配置先の考え方

| 配置先 | 性質 | 読み込まれるタイミング |
|--------|------|----------------------|
| `CLAUDE.md` | 常時効かせたい短い行動指示 | 毎セッション自動 |
| `DESIGN.md` | デザイン参照値・トークン | 必要時に参照 |
| `SKILL.md` | 繰り返す手順・ワークフロー | タスク発生時にオンデマンド |
| `.claude/rules/` 配下 | 特定ディレクトリにだけ効くルール | 該当パスのファイルを触るとき |

---

各ファイルの具体的な中身（`CLAUDE.md` / `DESIGN.md` / `SKILL.md` / `.claude/rules/` 配下）は、テンプレートに配置済みです。実体はそれぞれのファイルを直接参照してください（ここでは二重管理を避けるため転記しません）。

## ✅ 実装チェックリスト

### 1. プロジェクト構成

- [ ] Vite を導入する
- [ ] `.gitignore` を追加する
- [ ] `README.md` に Figma URL を記載する
- [ ] CSS はセクションごとにファイルを分ける
- [ ] リセット CSS または `box-sizing: border-box` を適用する
- [ ] stylelint / eslint / htmlhint / pre-commit を導入する

### 2. コーディング規約

- [ ] カラー・余白・サイズは `:root` 変数で管理し、直値を書かない
- [ ] class 名の命名規則は BEM で統一する
- [ ] 単位は `rem` / `px` を目的に応じて使い分け、混在させない
- [ ] フォントは `font-display: swap` を指定する

### 3. マークアップ（HTML）

- [ ] セクションごとに区切りコメントを追加する
- [ ] `div` を乱用せず、セマンティックタグを使う
- [ ] 見出しレベルを順序通りに使う
- [ ] `<meta name="viewport">` を必ず設定する
- [ ] ハンバーガーメニュー・アコーディオンの矢印などは SVG で出力する
- [ ] リンク・ボタンの遷移先（`href`）がすべて設定されている（`#`・空・ダミーのまま放置しない。未定なら TODO を明示）
- [ ] 外部リンクに `target="_blank"` + `rel="noopener noreferrer"` が付いている

### 4. 画像・アセット

- [ ] 画像は WebP で書き出す
- [ ] `<img>` には必ず `width` / `height` 属性を指定する
- [ ] `<img>` には必ず `alt` 属性を指定する
- [ ] `loading="lazy"` を追加する
- [ ] `srcset` で解像度ごとの画像を出し分ける
- [ ] 不要な背景・装飾（矩形など）が画像に焼き込まれていない（透過要否を確認）

### 5. レスポンシブ・インタラクション

- [ ] 値の伸縮（フォント・余白・最大幅）は `clamp()` で実装する
- [ ] カードの段組みは Grid auto-fit、タグ・ボタンの折り返しは `flex-wrap` を使う
- [ ] PC/SPで並び順が変わる箇所は `grid-template-areas` で切替（`display:contents`+`order` は使わない）
- [ ] レイアウト構造が根本的に変わる箇所のみメディアクエリを併用する
- [ ] ハンバーガーメニュー・アコーディオンには `aria` 属性を付ける
- [ ] イベントリスナーの重複登録を避ける
- [ ] CSS の `@import` を使っていない（HMRで更新されないため）

### 6. 品質検証

- [ ] PC・SP両方とも `get_design_context`（正データ）で読み込んだ（スクショ・推測で実装していない）
- [ ] 各セクションでFigmaとのスクリーンショット比較を行う
- [ ] computed style を実測し、正データと一致することを確認する（検証前にハードリロード）
- [ ] 細部（下線gap・重なり順・注記位置・文言）がFigmaと一致している
- [ ] Lighthouseでパフォーマンス・アクセシビリティ・SEOスコアを計測する
- [ ] プレビュー環境で実機確認を行う

### 7. SEO・Git

- [ ] すべての Figma データを読み込み、Title / description を生成する
- [ ] OGP タグ（`og:title` / `og:description` / `og:image`）を設定する
- [ ] push対象や共有時の参照はブランチ名ではなくコミットハッシュで固定する
- [ ] コミットはセクション単位で区切る

---

## 参考リンク

- Claude Code ベストプラクティス：https://code.claude.com/docs/ja/best-practices
- Claude Code プラグイン仕様：https://code.claude.com/docs/en/plugins
- Figma × Claude Code MCPサーバー設定：https://help.figma.com/hc/en-us/articles/39888612464151
